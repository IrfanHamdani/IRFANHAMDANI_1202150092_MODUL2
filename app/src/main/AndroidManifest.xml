<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.irfanhamdani_1202150092_modul2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuUtama" />
        <activity android:name=".TakeAwayMenu" />
        <activity android:name=".DineInMenu" />
        <activity android:name=".MenuList" />
        <activity android:name=".MenuDetail"
            android:parentActivityName=".MenuList">
            <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".MenuList" />
            </activity>
    </application>

</manifest>